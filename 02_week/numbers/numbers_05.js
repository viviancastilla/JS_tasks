const num = 5.67;

const roundedNum = Math.round(num);
const roundedNumDown = Math.floor(num);
const roundedNumUp = Math.ceil(num);

console.log(`The rounded value is ${roundedNum}`);
console.log(`The floored value is ${roundedNumDown}`);
console.log(`The ceiled value is ${roundedNumUp}`);